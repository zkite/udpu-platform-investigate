# Configuration for edpu build

CONFIG_VERSION_NUMBER="0.23.0"

CONFIG_CC_COMPILER="/opt/cross/aarch64-linux-musl-cross/bin/aarch64-linux-musl-gcc"

CONFIG_GOOS="linux"
CONFIG_GOARCH="arm64"

CONFIG_BUILD_FLAGS="-a -ldflags='-extldflags=-static'"

